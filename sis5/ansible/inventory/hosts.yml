---
all:
  children:
    frontend:
      hosts:
        vm1:
          ansible_host: 192.168.1.10  # Update with actual IP
          ansible_user: automation
    backend:
      hosts:
        vm2:
          ansible_host: 192.168.1.11  # Update with actual IP
          ansible_user: automation
  vars:
    ansible_python_interpreter: /usr/bin/python3
    project_name: dapmeet
    domain_name: dapmeet.local

